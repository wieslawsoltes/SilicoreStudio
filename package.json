{
  "name": "silicore-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free WebGPU EDA workbench with a transparent educational implementation kernel",
  "scripts": { "test": "node --test tests/*.test.js", "build": "node build.mjs", "start": "python3 -m http.server 8080" }
}
